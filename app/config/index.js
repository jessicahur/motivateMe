import resourceConfig from './resource-config';
/**
 * Bundle components in this dir. so that app can be passed to each.
 */
export default function(angularModule) {
	resourceConfig(angularModule);
}
